% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/replicates.R
\name{maxent_replicate}
\alias{maxent_replicate}
\title{Replicate Runs for Maxent Models}
\usage{
maxent_replicate(
  env_vals,
  sample_indices,
  num_points,
  n_replicates = 5L,
  replicate_type = "bootstrap",
  types = c("linear", "quadratic", "hinge"),
  n_hinges = 15L,
  max_iter = 500L,
  seed = 42L,
  categorical = NULL,
  bias_weights = NULL
)
}
\arguments{
\item{env_vals}{Named list of numeric vectors (one per environmental
variable, length = total points: background + occurrences).}

\item{sample_indices}{Integer vector: 0-based indices of occurrence
samples within \code{env_vals}.}

\item{num_points}{Integer: total number of points.}

\item{n_replicates}{Integer: number of replicates (default 5).}

\item{replicate_type}{Character: \code{"bootstrap"} (default) samples
with replacement, \code{"subsample"} uses a 75\% random subsample.}

\item{types}{Character vector of feature types (default
\code{c("linear", "quadratic", "hinge")}).}

\item{n_hinges}{Integer: number of hinge knots (default 15).}

\item{max_iter}{Integer: maximum training iterations (default 500).}

\item{seed}{Integer: random seed (default 42).}

\item{categorical}{Character vector of variable names that are
categorical (default \code{NULL}).}

\item{bias_weights}{Optional numeric vector of per-point bias weights.}
}
\value{
A named list with:
  \describe{
    \item{replicate_results}{List of per-replicate result lists.}
    \item{summary}{A \code{data.frame} with mean and sd statistics.}
  }
}
\description{
Trains multiple replicate models using either bootstrap sampling
or subsample splitting of occurrence data.  This mirrors Java
Maxent's \code{SampleSet.replicate()} method.
}
\examples{
\dontrun{
env <- list(temp = rnorm(110), precip = rnorm(110))
reps <- maxent_replicate(env, sample_indices = 100:109,
                          num_points = 110L, n_replicates = 5L)
print(reps$summary)
}
}
