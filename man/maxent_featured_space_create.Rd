% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{maxent_featured_space_create}
\alias{maxent_featured_space_create}
\title{Create a FeaturedSpace object}
\usage{
maxent_featured_space_create(
  num_points,
  sample_indices,
  feature_ptrs,
  bias_weights = numeric(0)
)
}
\arguments{
\item{num_points}{Integer: number of background points.}

\item{sample_indices}{Integer vector: 0-based indices of occurrence samples
in the background array.}

\item{feature_ptrs}{List of external pointers to Feature objects
(from \code{create_linear_feature()} etc.).}

\item{bias_weights}{Optional numeric vector of per-point bias weights
(length \code{num_points}).  When supplied, background density is
computed as \code{bias[i] * exp(lp[i] - lpn)} instead of the standard
\code{exp(lp[i] - lpn)}.  Pass an empty vector (default) for uniform
(unbiased) background.}
}
\value{
External pointer to a FeaturedSpace object.
}
\description{
Constructs a MaxEnt FeaturedSpace from background point count,
occurrence sample indices, and a list of Feature objects.
}
