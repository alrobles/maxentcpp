% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jackknife.R
\name{maxent_jackknife}
\alias{maxent_jackknife}
\title{Jackknife Variable Importance Analysis}
\usage{
maxent_jackknife(
  env_vals,
  sample_indices,
  num_points,
  types = c("linear", "quadratic", "hinge"),
  n_hinges = 15L,
  max_iter = 500L,
  categorical = NULL,
  bias_weights = NULL
)
}
\arguments{
\item{env_vals}{Named list of numeric vectors (one per environmental
variable, length = total points: background + occurrences).}

\item{sample_indices}{Integer vector: 0-based indices of occurrence
samples within \code{env_vals}.}

\item{num_points}{Integer: total number of points (background +
occurrences).}

\item{types}{Character vector of feature types (default
\code{c("linear", "quadratic", "hinge")}).}

\item{n_hinges}{Integer: number of hinge knots (default 15).}

\item{max_iter}{Integer: maximum training iterations (default 500).}

\item{categorical}{Character vector of variable names that are
categorical (default \code{NULL}).}

\item{bias_weights}{Optional numeric vector of per-point bias weights
(default \code{NULL}).}
}
\value{
A \code{data.frame} with columns:
  \describe{
    \item{variable}{Variable name.}
    \item{gain_without}{Training gain of the model with this variable
      excluded.}
    \item{gain_only}{Training gain of the model using only this
      variable.}
    \item{gain_full}{Training gain of the full model (same for all
      rows).}
  }
}
\description{
Performs a leave-one-variable-out jackknife analysis following the
procedure in Java Maxent's \code{Runner.jackknifeGain()}.  For each
environmental variable, two additional models are trained:
\enumerate{
  \item \strong{Leave-one-out}: the variable is excluded and the model
    is trained on all remaining variables.
  \item \strong{Only-one}: the model is trained using \emph{only} that
    single variable.
}
The resulting training gain and AUC are compared to the full model to
assess variable importance.
}
\examples{
\dontrun{
env <- list(
  temp  = rnorm(110),
  precip = rnorm(110),
  elev  = rnorm(110)
)
jk <- maxent_jackknife(env, sample_indices = 100:109,
                        num_points = 110L)
print(jk)
}
}
