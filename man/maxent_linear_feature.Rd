% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/features.R
\name{maxent_linear_feature}
\alias{maxent_linear_feature}
\title{Create a Linear Feature}
\usage{
maxent_linear_feature(values, name, min_val = NULL, max_val = NULL)
}
\arguments{
\item{values}{Numeric vector of environmental variable values.}

\item{name}{Character string: feature name/identifier.}

\item{min_val}{Minimum value for normalization. If \code{NULL} (default),
computed as \code{min(values)}.}

\item{max_val}{Maximum value for normalization. If \code{NULL} (default),
computed as \code{max(values)}.}
}
\value{
External pointer to a LinearFeature C++ object.
}
\description{
Creates a linear (normalized) feature that evaluates to
\code{(values[i] - min) / (max - min)}. Returns 0 when \code{min == max}.
}
\examples{
vals <- c(0, 5, 10, 3)
f <- maxent_linear_feature(vals, "temperature")
maxent_feature_eval(f, 1)  # index 1 (R) -> 0-based index 0
}
