% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maxent_outputs.R
\name{maxent_append_results_csv}
\alias{maxent_append_results_csv}
\title{Append a Row to maxentResults.csv}
\usage{
maxent_append_results_csv(
  results_file,
  species,
  n_training,
  n_test = 0L,
  training_gain,
  training_auc,
  test_gain = NA_real_,
  test_auc = NA_real_,
  entropy,
  contributions_df,
  perm_imp_df
)
}
\arguments{
\item{results_file}{Character: path to the results CSV file.}

\item{species}{Character: species name.}

\item{n_training}{Integer: number of training presence points.}

\item{n_test}{Integer: number of test presence points (default 0).}

\item{training_gain}{Numeric: regularized training gain.}

\item{training_auc}{Numeric: training AUC.}

\item{test_gain}{Numeric or \code{NA}: regularized test gain.}

\item{test_auc}{Numeric or \code{NA}: test AUC.}

\item{entropy}{Numeric: model entropy.}

\item{contributions_df}{A data.frame with columns \code{name} and
\code{contribution} (from \code{\link{maxent_percent_contribution}}).}

\item{perm_imp_df}{A data.frame with columns \code{name} and
\code{permutation_importance} (from
\code{\link{maxent_permutation_importance}}).}
}
\value{
Invisibly returns \code{results_file}.
}
\description{
Appends one row of species-level summary statistics to a
\code{maxentResults.csv} file, creating the file (with a header) if it does
not yet exist.  Column names match the Java Maxent output for
cross-tool compatibility.
}
\examples{
\dontrun{
maxent_append_results_csv(
  file.path(output_dir, "maxentResults.csv"),
  species = "Sp1", n_training = 50L, training_gain = 1.23,
  training_auc = 0.95, entropy = 6.7,
  contributions_df = contrib, perm_imp_df = perm_imp)
}
}
