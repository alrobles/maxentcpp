% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/features.R
\name{maxent_quadratic_feature}
\alias{maxent_quadratic_feature}
\title{Create a Quadratic Feature}
\usage{
maxent_quadratic_feature(values, name, min_val = NULL, max_val = NULL)
}
\arguments{
\item{values}{Numeric vector of environmental variable values.}

\item{name}{Character string: feature name/identifier.}

\item{min_val}{Minimum value for normalization. If \code{NULL} (default),
computed as \code{min(values)}.}

\item{max_val}{Maximum value for normalization. If \code{NULL} (default),
computed as \code{max(values)}.}
}
\value{
External pointer to a QuadraticFeature C++ object.
}
\description{
Creates a quadratic feature that evaluates to \code{linear_val^2} where
\code{linear_val = (values[i] - min) / (max - min)}.
}
\examples{
\dontrun{
vals <- c(0, 5, 10)
f <- maxent_quadratic_feature(vals, "temperature_sq")
maxent_feature_eval(f, 2)  # evaluates at index 2 (1-based → 0-based: 1)
}
}
