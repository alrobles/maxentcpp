# Get layer metadata

Get layer metadata

## Usage

``` r
get_layer_info(layer_ptr)
```

## Arguments

- layer_ptr:

  External pointer to a Layer object.

## Value

Named list with name and type (string).
